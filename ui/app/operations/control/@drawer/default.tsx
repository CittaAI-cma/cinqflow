/** No drawer open. A parallel slot must render something on every route that
 *  does not intercept, or the slot is dropped on a hard navigation. */
export default function NoDrawer() {
  return null;
}
